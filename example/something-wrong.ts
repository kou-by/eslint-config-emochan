const x1f = /\x1f/;

console.log(x1f);

export const myname = 'emochan';

export enum Foo {
  bar = 1,
}

export const obj = {
  myname: myname,
};

if (false) {
}

export function createFuture(x: string) {
  return null;
}
